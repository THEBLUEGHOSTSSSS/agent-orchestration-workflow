# Contributing

感谢改进 `agent-orchestration-workflow`。本项目欢迎对政策清晰度、安全边界、模板可操作性、静态验收检查器、示例和翻译的修订。

## 提交原则

- 保持根目录 `AGENTS.md` 与解释性文档的一致性；涉及策略含义的改变应明确说明动机和风险。
- 不加入凭据、个人绝对路径、真实客户名称、私有历史、供应商端点或未经许可的材料。
- 不声称未经实际测量的成本、速度、质量或模型性能结果。
- 示例必须明确标记为说明性内容，并使用虚构项目和假设输出。
- 不提交 `codex-worker` 的运行时实现或用测试机械复述自然语言政策。
- 保持本项目供应商中立，并明确它不是官方厂商产品。

## 修改流程

1. 在专用分支或工作区进行修改。
2. 检查所有相对 Markdown 链接都指向仓库内存在的文件。
3. 搜索凭据形态、个人路径、真实组织名称和未经证实的量化主张。
4. 对照 `AGENTS.md` 检查角色边界、两轮上限、一次打回、指挥者接管、必需验收与经验记录，以及安全禁止项。
5. 检查器代码发生变化时运行 `python3 -m unittest discover -s tests -v`，覆盖真实边界行为；纯说明编辑检查链接、命令和语义一致性即可。
6. 由指挥者核对实际改动，在项目记录中留下验收判断、经验与证据；同步中英文 README，必要时更新 [CHANGELOG.md](CHANGELOG.md)。
7. 在变更说明中列出实际运行的检查及未运行项。

本地贡献准备完成不代表获得外部发布权限。提交、推送或创建拉取请求应遵循贡献者所在环境的授权流程。

## 文档风格

中文 README 是主要入口，英文 README 保持简洁但语义一致。使用具体、可验证的措辞；把事实、建议、示例和不确定性区分开。链接优先使用相对路径，命令应可复制且不依赖个人目录。

提交贡献即表示你同意按本仓库的 MIT License 提供该贡献。
