# 本地验收：强模型审核与经验闭环升级

日期：2026-09-23。指挥者：本次主会话。基线：`77e19fafb40454ce0e1f0e95bef6567b6d60403b`。

## 执行与接管

任务 `UPGRADE-DOCS-20260923` 消耗 1 次外部 Worker 执行。Worker 修改了范围内文档、模板与示例，但没有返回最终报告。指挥者约在启动 12 分钟后主动终止该任务进程树，观察到退出码 143，按提前接管处理。没有发起第二次调用，也没有将中断标记为成功。

指挥者检查落盘差异，直接修正：旧流程图未限定返工次数、handoff 中基线仍标 optional、快速调用示例缺少停止条件，以及文档未同步新检查器的交付状态。指挥者另外完成 AGENTS 规则、详细说明、静态检查器与边界测试。没有第二模型独立评审指挥者代码；此处为主会话代码审查加可运行测试。

## 已运行检查与观察

| 检查 | 观察 |
| --- | --- |
| `python3 -m unittest discover -s tests -v` | 16 tests，OK。正常直接验收、第一/二轮成功和指挥者接管后通过；第三轮、重复打回、缺少经验、未运行检查、旧哈希、缺失文件、目录越界、符号链接越界和畸形记录被拒绝 |
| 未执行示例的 CLI 检查 | `docs/review-record.example.json` 返回 NOT ACCEPTED、退出码 1；没有将占位数据当真实验收 |
| JSON 与契约引用核对 | JSON 可解析；两个 goal/task 示例的目标 ID、版本一致；任务保持 pending、空执行历史；两轮策略字段准确 |
| Markdown 相对文件链接检查 | 当前公开交付范围内无缺失本地文件链接 |
| `git diff --check` | 无空白错误 |
| 公开范围检查 | 没有匹配个人绝对路径、私钥头和常见访问令牌模式；人工检查新增文件，没有纳入私有运行日志、配置或 research 目录 |
| 策略复核 | 指挥/审核职责、最多两轮、最多一次打回、指挥者接管、必需验收和必需经验一致；不绑定某个模型名 |

## 代码复核与边界

检查器只读取 JSON 和 root 内的文件，核对 SHA-256；不执行记录里的命令，不联网，不调用模型，不接受跳出 root 的路径。结构失败返回非零。语义正确性、真实执行次数、审核者身份与能力不能由这份自报记录证明；故文档明确要求指挥者核对，且不把脚本称为调度器或不可绕过的权限控制。

测试覆盖的是静态验收行为。全流程节约率、未来会话的实际遵守率、自动运行时拦截、递归 Worker 和自动模型切换均未测试或未实现。本次没有改变模型、认证或私有 Worker 配置。

本记录接受本地工作流制品及项目经验，文件摘要见同目录验收 JSON。全局安装与 GitHub 发布身份核对在交付阶段另行执行；本记录不预先宣称远端发布成功。人的最终验收与否决权保持不变。
